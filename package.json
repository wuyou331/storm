{
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "jest": "^27.3.1",
    "ts-jest": "^27.0.7",
    "ts-node": "^10.3.0",
    "tst-expression-transformer": "^1.1.0",
    "ttypescript": "^1.5.12",
    "typescript": "^4.4.4"
  },
  "dependencies": {
    "tst-expression": "^1.0.2"
  },
  "babel": {
    "presets": [
      "@babel/preset-env",
      {
        "targets": {
          "node": "current"
        }
      },
      "@babel/preset-typescript"
    ]
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": [
      "**/?(*.)(test).ts"
    ],
    "globals": {
      "ts-jest": {
        "compiler": "ttypescript"
      }
    }
  }
}