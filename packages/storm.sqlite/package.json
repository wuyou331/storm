{
    "name": "storm.sqlite",
    "version": "0.0.1",
    "scripts": {
        "test": "jest"
    },
    "dependencies": {
        "@types/sqlite3": "^3.1.7",
        "sqlite3": "^5.0.2",
        "storm": "0.0.1"
    },
    "jest": {
        "preset": "ts-jest",
        "testEnvironment": "node",
        "testMatch": [
            "**/?(*.)(test).ts"
        ],
        "globals": {
            "ts-jest": {
                "compiler": "ttypescript"
            }
        }
    }
}